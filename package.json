{
  "name": "hoosfunding",
  "version": "0.1.0",
  "private": true,
  "description": "HoosFunding – demo full-stack app for collecting funding requests and AI-driven prioritization.",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "lint": "npm run lint --prefix client",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}

